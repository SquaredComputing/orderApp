from django.db import models

# Create your models here.
class OrderNum(models.Model):
    IdNum = models.

    def __init__(self, arg):
        superOrderNum, self).__init__()
        self.arg = arg
